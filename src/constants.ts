/**
 * Единый источник правды для всей сцены.
 *
 * ПРАВИЛО: ни один предмет не объявляет свой цвет, свой радиус скругления
 * и свои единицы. Всё резолвится отсюда. Консистентность должна быть
 * структурной, а не вопросом вкуса — иначе на двенадцатом предмете
 * комната развалится на двенадцать несогласованных объектов.
 *
 * ЕДИНИЦЫ: метры. Везде. Без исключений.
 *
 * TODO(макс): размеры ниже сняты на глаз с фотографий. Их надо перемерить
 * рулеткой — пропорция это первое, что читается как «ненастоящее».
 * Помечено ESTIMATED всё, что не измерено.
 */

/** Скругление рёбер. У реальных предметов нет идеально острых кромок:
 *  фаска ловит свет и отделяет «изготовленное» от «сгенерированного».
 *  Это самый дешёвый рычаг правдоподобия во всей сцене. */
export const BEVEL = {
  /** Мебель, корпуса, крупные плоскости */
  lg: 0.012,
  /** Средние детали: кронштейны, полки */
  md: 0.006,
  /** Мелочь: рёбра радиатора, тонкие профили */
  sm: 0.003,
} as const

/** Сегментация фаски. 2 достаточно на глаз и втрое дешевле по треугольникам. */
export const BEVEL_SEGMENTS = 2

/**
 * Палитра.
 *
 * ПОЧЕМУ ОНА ПЕРЕПИСАНА. Прежние значения снимались пипеткой с фотографий
 * комнаты, и это оказалось ловушкой: с фотографии снимается не краска, а
 * КРАСКА ПОД СВЕТОМ, плюс то, что с ней сделал автобаланс белого. Замер по
 * старой палитре: девять светлых поверхностей укладывались в полосу тона
 * шириной 11 градусов (H 37–48), а всё цветное в комнате — в клин H 4–48,
 * то есть в одну шестую круга. Холодного альбедо не было ни одного.
 *
 * Отсюда и бралась монохромность, которую не лечило освещение. Свет уже
 * честно менял цвет — ночью синий, на закате оранжевый (`main.ts`,
 * SKY_NIGHT/SKY_GOLDEN/SUN_GOLDEN). Но освещённость ПЕРЕМНОЖАЕТСЯ с
 * альбедо: на поверхности, у которой в самой краске сидит тепло, тёплый
 * свет даёт тёплое, а холодный — грязное. Разницы между временем суток не
 * появлялось, потому что её негде было записать.
 *
 * ПРАВИЛО НОВОЙ ПАЛИТРЫ: тепло приносит СВЕТ, а не краска.
 * Крашеная стена и потолочная эмаль — это S 0.02–0.06, а не 0.19–0.21, и
 * нынешние значения к правде БЛИЖЕ прежних, а не дальше. Цвет в комнате
 * держат три вещи и только они: тёплый паркет, красное кресло и холодная
 * пара «задняя стена / вещи на столе». Между ними и лежит кадр.
 *
 * Значения в linear-sRGB не переводим — three сделает это сам
 * при setHex/`SRGBColorSpace`.
 *
 * Проверяется скриптом: `bun run src/lib/palette.check.ts`.
 */
export const PALETTE = {
  // --- Оболочка ---
  /** Левая стена. Тёплая, но это уже ОТТЕНОК белого, а не слоновая кость. */
  wallWarm: 0xe9e6e1,
  /**
   * Задняя стена — та, в которой окно. Единственная плоскость комнаты,
   * куда прямое солнце не попадает никогда: она всё время смотрит внутрь,
   * и заполняет её небо. Холодная краска здесь работает вместе с этим,
   * а не против — и она же единственный крупный холодный кусок кадра,
   * которому есть что противопоставить паркету.
   */
  wallCool: 0xc6c9cd,
  /** Потолок ловит небо, а не пол, — поэтому холоднее левой стены.
   *  Пять единиц из 255 в минус: меньше — и подпись врёт, плоскость
   *  выходит ровной. Проверяется столбцом «подтон» в palette.check. */
  ceiling: 0xedeff2,
  floorOak: 0xb8845a, // медовый паркет — главный тёплый в комнате
  floorOakDark: 0x9c6c46, // вариация досок
  /** Плинтус БЕЛЕЕ стены: он крашен эмалью, и это его главный признак.
   *  Ровно нейтральный — якорь, относительно которого видно, что стена
   *  тёплая, а потолок холодный. */
  skirting: 0xf1f1f1,

  // --- Стол ---
  deskTop: 0xf1f2f2,
  /** Крашеный металл рамы холоднее ЛДСП столешницы — так и в жизни. */
  deskFrame: 0xd8dade,

  // Радиатор: панельные красят белым, а не сливочным. Он стоит под окном,
  // то есть в самом холодном свете комнаты, — и краска ему не мешает.
  radiator: 0xf0f2f5,

  // Лампа. Сама она БЕЛАЯ — красные только настенный кронштейн и ремешок.
  // Красный тут работает точечным акцентом, а не цветом предмета:
  // если покрасить руку целиком, лампа начинает орать на всю комнату.
  lampArm: 0xececeb,
  lampShade: 0xf4f4f2,
  /** Изнанка раструба — единственное место, где тёплая КРАСКА оправдана:
   *  это отражённый свет лампочки, а не цвет плафона. */
  lampShadeInner: 0xfff6e8,
  accentRed: 0xc4372c,

  // --- Техника ---
  screenBezel: 0x1c1c1e,
  screenBody: 0x2a2a2d,
  screenOff: 0x0d0d10,
  macbookBody: 0xc9a396, // розовое золото
  macbookScreen: 0x14141a,

  // --- Книга и планшет ---
  // Холодная пара на столе. Она и есть второй полюс: без неё паркет с
  // креслом остаются единственным цветом в кадре, и комната сводится
  // к одной краске, каким бы ни был свет.
  /** Переплёт книги: тканевый, глубокий сине-зелёный. */
  bookCloth: 0x2f5d63,
  /** Обрез страниц. Тёплый — и именно рядом с холодным переплётом это
   *  читается как бумага, а не как жёлтая полоска. */
  bookPages: 0xe8e2d2,
  /** Чехол-подставка планшета: родня переплёту, но темнее и глуше. */
  tabletCase: 0x2c4f52,
  tabletBody: 0x3a3d42,

  // --- Общее ---
  metal: 0x8a8a8a,
  metalDark: 0x4a4a4a,
  rubber: 0x2b2b2b,
} as const

/** Габариты комнаты. Начало координат — угол «левая стена × задняя стена × пол».
 *  X — вправо вдоль задней стены. Z — на зрителя. Y — вверх. */
export const ROOM = {
  width: 3.4, // ESTIMATED
  depth: 3.2, // ESTIMATED
  height: 2.5, // ESTIMATED
  wallThickness: 0.12,
  skirtingHeight: 0.07,
} as const

/** Окно в задней стене — основной источник света, поэтому важно для бейка. */
export const WINDOW = {
  width: 1.3, // ESTIMATED
  height: 1.25, // ESTIMATED
  sill: 0.95, // ESTIMATED, высота подоконника от пола
  offsetX: 2.15, // ESTIMATED, центр окна от левого угла
  reveal: 0.06, // глубина откоса
} as const

/** Стол с электроподъёмом. */
export const DESK = {
  width: 1.4, // ESTIMATED
  depth: 0.7, // ESTIMATED
  topThickness: 0.025,
  height: 0.735, // ESTIMATED, положение «сидя» как на фото
  legInset: 0.08, // отступ ноги от торца столешницы
  columnOuter: 0.075, // внешняя секция телескопической ноги
  columnInner: 0.058, // внутренняя секция
  footLength: 0.62,
  footHeight: 0.045,
  // posX — это ЦЕНТР стола. Он обязан быть не меньше width/2 плюс зазор,
  // иначе левый торец вместе с ногой уезжает внутрь стены (и нога исчезает
  // из кадра, не вызывая никакой ошибки — молчаливый провал).
  posX: 0.8, // ESTIMATED
  posZ: 0.42, // ESTIMATED, от задней стены
} as const

/** Панельный радиатор под окном. */
export const RADIATOR = {
  width: 1.1, // ESTIMATED
  height: 0.58, // ESTIMATED
  depth: 0.1, // ESTIMATED
  floorGap: 0.12,
  finCount: 22,
  finDepth: 0.055,
  topGrilleSlots: 18,
} as const

/**
 * Белая шарнирная лампа на красном основании, стоит на столе.
 * Самый сложный объект комнаты и честный индикатор всего подхода.
 */
export const LAMP = {
  /** Лампа стоит НА СТОЛЕ, слева от монитора, а не на стене.
   *  Координаты локальные для группы workstation — то есть относительно
   *  ЦЕНТРА столешницы, поэтому лампа ездит и крутится вместе со столом. */
  deskX: -0.55, // ESTIMATED, левее монитора
  deskZ: -0.24, // ESTIMATED, у задней кромки стола
  /** Разворот основания. Рука вылетает вдоль локального +X, поэтому без
   *  поворота плафон приезжает ровно в монитор (ловилось checkLayout).
   *  −1.0 уводит вынос по диагонали вперёд-вправо, над свободной
   *  частью столешницы. */
  yaw: -1.0,
  baseRadius: 0.082,
  baseHeight: 0.022,
  postHeight: 0.1,
  armLower: 0.44, // ESTIMATED
  armUpper: 0.4, // ESTIMATED
  armThickness: 0.014,
  armGauge: 0.028, // расстояние между парными тягами параллелограмма
  springTurns: 22,
  springRadius: 0.011,
  shadeRadius: 0.115, // ESTIMATED
  shadeHeight: 0.1,
  /**
   * Углы покоя шарниров (радианы). КУМУЛЯТИВНЫЕ по цепочке: каждый
   * следующий отсчитывается от уже повёрнутого родителя. Сумма всех трёх
   * обязана быть ≈ 0, иначе плафон висит косо — раструб должен смотреть
   * строго вниз. Настольная поза: сначала крутой подъём от основания,
   * затем вынос почти горизонтально над столешницей.
   */
  angleLower: -0.35,
  angleUpper: -1.25,
  angleShade: 1.6,
} as const

/** Изогнутый ультраширокий монитор (MSI 34", 21:9, кривизна 1500R). */
export const MONITOR = {
  screenW: 0.8, // ESTIMATED, видимая ширина полотна
  screenH: 0.34, // ESTIMATED
  curveRadius: 1.5, // 1500R — паспортная кривизна, отсюда и дуга
  bezel: 0.008,
  panelDepth: 0.022,
  neckH: 0.17,
  baseW: 0.30,
  baseD: 0.22,
  posX: 0.72, // ESTIMATED, на столе
  posZ: 0.2, // ESTIMATED, ближе к задней кромке
} as const

/**
 * MacBook Air M1 13", розовое золото, открыт.
 * Габариты паспортные (30.41 × 21.24 × 1.61 см), не на глаз —
 * поэтому силуэт попадает без подгонки.
 */
export const MACBOOK = {
  w: 0.3041,
  d: 0.2124,
  baseH: 0.0161,
  lidT: 0.0042,
  /** Видимая матрица 13.3" */
  screenW: 0.286,
  screenH: 0.179,
  bezel: 0.009,
  /** Клавиатурная площадка */
  kbW: 0.228,
  kbD: 0.108,
  keyCols: 14,
  keyRows: 5,
  /** Трекпад */
  padW: 0.126,
  padD: 0.081,
  /** Угол раскрытия крышки от горизонтали (радианы) */
  lidAngle: 1.85,
  posX: 1.19, // ESTIMATED
  posZ: 0.45, // ESTIMATED
  yaw: -0.22, // ESTIMATED, слегка развёрнут к креслу
} as const

/**
 * Книга в твёрдом переплёте, закрытая, лежит на столе.
 *
 * Габариты — обычного романа в твёрдой обложке (216 × 140 мм), а не «на
 * глаз»: у книги пропорция узнаваемая, и ошибка в ней читается мгновенно.
 * Переплётные крышки выступают за блок на «кант» — три миллиметра по
 * трём сторонам. Без канта получается брусок, а не книга.
 */
export const BOOK = {
  /** Высота страницы (вдоль корешка). */
  h: 0.216,
  /** Ширина страницы (от корешка к обрезу). */
  w: 0.14,
  /** Толщина книжного блока без крышек. */
  block: 0.028,
  /** Толщина переплётной крышки. */
  board: 0.0022,
  /** Кант: насколько крышка выступает за блок. */
  square: 0.003,
  /**
   * Стрелка дуги корешка: насколько он выступает за кромку крышки.
   *
   * Семь миллиметров при толщине книги в 32 — это пологая дуга примерно
   * в 94°, как у настоящего кругленного блока. Половина толщины (то есть
   * полуцилиндр) дала бы валик и лишние 16 мм ширины.
   */
  spineBulge: 0.007,
  /** Ширина ленточки-закладки. */
  ribbon: 0.008,
  /** Насколько ленточка вылезает из блока. */
  ribbonOut: 0.035,
  /**
   * Название на корешке. Дано Максимом 2026-08-13.
   *
   * Автора нет намеренно: он не назван, а придумывать имя живому автору
   * к чужому названию — ровно то, чего это поле и избегало, пока стояло
   * пустым. Корешок с одним названием честен; корешок с выдуманным
   * автором — нет.
   *
   * `null` здесь по-прежнему рабочее состояние: без названия корешок
   * собирается гладким, без текстуры и без лишнего холста.
   */
  title: 'Geek Profile' as string | null,
  author: null as string | null,
} as const

/**
 * iPad Air 11" (M2) в чехле-подставке, стоит на столе портретом.
 *
 * Габариты ПАСПОРТНЫЕ: 247.6 × 178.5 × 6.1 мм. Видимая матрица считается
 * из диагонали 10.86" и разрешения 2360 × 1640 — отсюда рамка выходит
 * ровной со всех сторон (10.55 мм), как и должна быть у планшета.
 * Подгонять её на глаз нельзя: неровная рамка — первое, что выдаёт
 * «нарисованный» гаджет.
 *
 * ПОРТРЕТ, А НЕ ЛАНДШАФТ. Планшет, стоящий боком, читается как второй
 * маленький монитор. Стоящий портретом — сразу как планшет. И интерфейс
 * «сейчас играет» вертикальный по своей природе: обложка, название,
 * дорожка, кнопки — одна колонка.
 */
export const TABLET = {
  w: 0.1785,
  h: 0.2476,
  thickness: 0.0061,
  screenW: 0.1574,
  screenH: 0.2265,
  bezel: 0.01055,
  /** Толщина задней крышки чехла. */
  caseBack: 0.0018,
  /** Угол наклона от вертикали: чехол-подставка держит планшет откинутым
   *  назад примерно на четверть радиана. Прямо стоящий планшет читается
   *  как приклеенный к столу. */
  tilt: 0.28,
} as const

/**
 * Рабочее место как единый узел: стол, монитор, ноутбук и кресло стоят
 * друг относительно друга жёстко, а разворачиваются и двигаются вместе.
 * Без этой группы поворот стола на 90° потребовал бы вручную
 * пересчитывать позиции всех предметов на нём.
 *
 * yaw = −π/2 разворачивает стол вдоль ЛЕВОЙ стены, так что окно
 * и радиатор оказываются справа от него — как в комнате.
 */
export const WORKSTATION = {
  // ЗНАК ВАЖЕН. При yaw = +π/2 локальная ось +Z стола (та сторона, где
  // сидят) уходит в мировой +X, то есть в комнату. При −π/2 стол
  // разворачивается лицом в левую стену: монитор и ноутбук смотрят
  // в штукатурку, а видно только их затылки.
  yaw: Math.PI / 2,
  posX: 0.62, // ESTIMATED, отступ от левой стены
  posZ: 1.15, // ESTIMATED, от задней стены
} as const

/**
 * Красное сетчатое эргономичное кресло — второй по узнаваемости предмет
 * комнаты после лампы. Ярко-красная трубчатая рама, тёмная сетка,
 * подголовник на стойке, серые подлокотники, пятилучевая база.
 */
export const CHAIR = {
  seatH: 0.47, // ESTIMATED, высота сиденья
  seatW: 0.48,
  seatD: 0.46,
  backH: 0.62, // ESTIMATED, высота спинки от сиденья
  backW: 0.44,
  headrestH: 0.2,
  headrestW: 0.3,
  postH: 0.16, // стойка подголовника
  tubeR: 0.014, // радиус трубы рамы
  baseR: 0.32, // вылет луча базы
  casterR: 0.028,
  legs: 5,
  /** Поворот кресла относительно рабочего места. Спинка кресла собрана
   *  со стороны локального −Z, поэтому чтобы кресло смотрело НА стол,
   *  его надо развернуть на π плюс лёгкий бытовой угол. */
  yaw: Math.PI + 0.22,
  offsetX: 0.06, // ESTIMATED, лёгкий сдвиг вдоль стола
  offsetZ: 0.63, // ESTIMATED, задвинуто за стол с рабочей стороны
} as const

/** Камера спайка. Подбирается под конкретный кадр с фотографии,
 *  чтобы можно было положить рендер и фото рядом. */
export const SPIKE_CAMERA = {
  position: [3.15, 1.75, 3.0] as const,
  target: [1.0, 0.95, 0.3] as const,
  fov: 52, // ESTIMATED, надо подогнать под фото точнее
} as const
